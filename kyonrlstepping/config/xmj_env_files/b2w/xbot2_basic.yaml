XBotInterface:
  urdf_path: $PWD/unitree_b2w.urdf
  srdf_path: $PWD/unitree_b2w.srdf

ModelInterface:
  model_type: pin
  is_model_floating_base: true

# defaults
motor_pd:
  "*hip_joint": [200, 60]
  "*thigh_joint": [200, 60]
  "*calf_joint": [200, 60]
  "*foot_joint": [0, 30]

# startup pd config
startup_motor_pd:
  "*hip_joint": [200, 60]
  "*thigh_joint": [200, 60]
  "*calf_joint": [200, 60]
  "*foot_joint": [0, 30]
  
# hal
xbotcore_device_configs:
    sim: $PWD/hal/b2w_gz.yaml
    dummy: $PWD/hal/b2w_dummy.yaml

# threads
xbotcore_threads:
    rt_main:  {sched: fifo , prio: 60, period: 0.001}
    nrt_main: {sched: other, prio: 0 , period: 0.005}

# plugins
xbotcore_plugins:

    homing:
        thread: rt_main
        type: homing

    ros_io: {thread: nrt_main, type: ros2_io}

    zmq_io:
        thread: nrt_main
        type: zmq_io
        params:
          autostart: {type: bool, value: true}
          protocol: {type: string, value: tcp}
          tcp_state_port: {type: int, value: 5559}
          tcp_cmd_port: {type: int, value: 5558}
          tcp_service_port: {type: int, value: 5557}
          ipc_state_path: {type: string, value: /tmp/xbot2_zmq_pub.ipc}
          ipc_cmd_path: {type: string, value: /tmp/xbot2_zmq_cmd.ipc}
          ipc_service_path: {type: string, value: /tmp/xbot2_zmq_rep.ipc}

# global parameters
xbotcore_param:
  /xbot/hal/joint_safety/filter_autostart: {value: true, type: bool}
  /xbot/hal/joint_safety/filter_cutoff_hz: {value: 2.0, type: double}
  /xbot/hal/enable_safety: {value: false, type: bool}
